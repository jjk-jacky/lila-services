#!@BINDIR@/execlineb -P
aa-service
s6-envdir .
unexport run
importas -D 38400 baud baud
backtick -n options { ifte
  { importas options options echo ${options} ${INSTANCE} }
  { echo ${INSTANCE} }
  aa-test -r options
}
importas -s options options
emptyenv
export TERM linux
s6-envdir /etc/anopa/env
agetty ${options} ${baud}
